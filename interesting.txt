     return /^[A-Z]/.test(str) && /\d+/.test(str) && /[^$].{6,}/.test(str); 
     // matches a string that starts with a Capital , have at least once number , and is 6 or more charcters

     
/^[a-zA-Z0-9]{6}$/.test(str)  
// match EXACTLY 6 charchters from chosen range


/^.{8}$/.test(str) 
// match string with ANY CHARS which have EXACTLY 8 chars. 

^[0-9]{9}$

/validate a phone number



^[^@]+@[^@.]{3,}.[a-zA-Z]{2,}
^[^@]+@[^@.]+\.[a-zA-Z]+$

//validate a correo
